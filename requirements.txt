# Core dependencies
pyyaml>=6.0
jinja2>=3.1.0
python-docx>=1.1.0
pdfkit>=1.0.0
markdown>=3.5.0

# Data processing
pandas>=2.0.0
nltk>=3.8.0

# Playwright for autofill
playwright>=1.40.0

# Optional: Local LLM support
llama-cpp-python>=0.2.0; platform_machine != "wasm32"

# CLI and UI
click>=8.1.0
rich>=13.0.0

# Web API (for Vercel deployment)
flask>=3.0.0

# Database
# Note: sqlite3 is part of Python's standard library (included by default)
# and should NOT be listed as a pip dependency
